// Get the RSA button element
const rsaButton = document.getElementById('rsa-button');

// Add a click event listener to the RSA button
rsaButton.addEventListener('click', function() {
   // Replace this with your actual RSA call code or open a phone dialer
   alert('Calling RSA...');
});
